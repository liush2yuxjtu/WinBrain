# MiniMax Anthropic Messages API verification

This file triggers the authenticated frontend recording. Both chat and skill-draft calls must return `usedLiveModel: true` with provider `minimax-anthropic-messages`.
