# Agent SDK recording verification

This temporary verification change triggers the same-repository pull-request workflows after the frontend recording job was switched from its deterministic SDK stub to the real Agent SDK dependency and encrypted `ANTHROPIC_API_KEY` injection.

No credential value is stored in this repository.
